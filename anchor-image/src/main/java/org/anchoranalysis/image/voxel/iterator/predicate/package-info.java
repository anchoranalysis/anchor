/**
 * Functional-interfaces for processing different combinations of points and buffers that return a boolean.
 *
 * <p>These interfaces are called from the iterators within {@code
 * org.anchoranalysis.image.voxel.iterator}.
 * 
 * <p>See {@code org.anchoranalysis.image.voxel.iterator.process} for similar interfaces that do not return a value
 */
package org.anchoranalysis.image.voxel.iterator.predicate;
